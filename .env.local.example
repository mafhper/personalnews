# RSS2JSON API Configuration
# Get a free API key from: https://rss2json.com
# Without an API key, RSS2JSON is rate-limited (temporary failures)
# With an API key, you get much higher rate limits and priority
VITE_RSS2JSON_API_KEY=your-api-key-here

# CorsProxy.io API Configuration (Optional)
# Get an API key from: https://corsproxy.io
# Free tier available - API key increases rate limits and gives you a dashboard
VITE_CORSPROXY_API_KEY=

# CodeTabs - Free CORS proxy (no auth required)
# Status: Excellent, very reliable, recommended
VITE_CODETABS_ENABLED=true

# AllOrigins - Simple & fast CORS proxy
# Status: Good reliability, cache-friendly
VITE_ALLORIGINS_ENABLED=true

# WhateverOrigin - Community maintained CORS proxy
# Status: Fair reliability, fallback option
VITE_WHATEVERORIGIN_ENABLED=true

# CORS Anywhere - Popular but unstable
# Status: Unreliable - use only as absolute last resort
VITE_CORS_ANYWHERE_ENABLED=true

# TextProxy - Text-focused proxy
# Status: Fair reliability, minimal maintenance
VITE_TEXTPROXY_ENABLED=true

# Jina Reader - Content normalization
# Website: https://jina.ai/
# Status: Excellent reliability, good for HTML normalization
VITE_JINA_READER_ENABLED=true

# Development Only: Prefer Local Proxy
# Set to 'true' to use the local development proxy at /local-proxy/ instead of remote proxies
# This option is only visible and functional in development mode
# For production (GitHub Pages), remote proxies are used as fallback
VITE_PREFER_LOCAL_PROXY=false

# Enable Proxy Debugging (optional)
# Shows detailed logs about which proxy is being used
VITE_PROXY_DEBUG=false

# ============================================================================
# HOW TO USE:
# ============================================================================
# 1. Copy this file to .env.local in the root directory
#    cp .env.local.example .env.local
#
# 2. (RECOMMENDED) Get a free RSS2JSON API key from https://rss2json.com
#    Replace VITE_RSS2JSON_API_KEY with your actual key
#    - Free tier: 30 requests/day
#    - With API key: 1000 requests/day + priority
#
# 3. (OPTIONAL) Get a CorsProxy.io API key from https://corsproxy.io
#    Replace VITE_CORSPROXY_API_KEY for better limits
#
# 4. The app will load your configuration automatically
#
# 5. For development: set VITE_PREFER_LOCAL_PROXY=true to test local proxy
#    (requires local proxy server running)
#
# IMPORTANT NOTES:
# - Never commit .env.local to git (already in .gitignore)
# - API keys are sensitive - keep them private
# - The app uses multiple proxies in a fallback chain for reliability
#
# For detailed information about each proxy, see: config/proxyConfig.ts
